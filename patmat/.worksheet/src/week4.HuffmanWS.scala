package week4

import patmat.Huffman._

import common._

object HuffmanWS {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(118); 
 
	val ch =  string2Chars("afbsbbcdaaacb");System.out.println("""ch  : List[Char] = """ + $show(ch ));$skip(28); val res$0 = 
	
   decodedSecret.mkString;System.out.println("""res0: String = """ + $show(res$0))}
  
  
	
}
