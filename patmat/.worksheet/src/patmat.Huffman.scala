package patmat

object Huffman {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(44); val res$0 = 
  
  List();System.out.println("""res0: List[Nothing] = """ + $show(res$0));$skip(10); val res$1 = 
  List(1);System.out.println("""res1: List[Int] = """ + $show(res$1));$skip(13); val res$2 = 
  List(2, 3);System.out.println("""res2: List[Int] = """ + $show(res$2))}
}
