package week6

import forcomp.Anagrams._

import sun.org.mozilla.javascript.ast.Yield

object Anagrams {

  val word: Word = "eat"                          //> word  : forcomp.Anagrams.Word = eat
  
 /* def subtract(x: Occurrences, y: Occurrences): Occurrences = {
  	val xMap = x.toMap
  	for (v <- y) {
  		xMap.get(v._1) match {
  			case Some(k) => xMap + (v._1, k + v._2)
  		}
  	}
  }
  
  val aa = List(('a', 2))
  val a = List(('a', 1))
    
  subtract(aa, a)
  
  aa.toMap
  */
  }